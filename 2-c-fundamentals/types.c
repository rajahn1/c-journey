#include <stdio.h>

int main() {

  float a, b, c, d, e;
  int f, g, h, i;

  //  printf("%d %d %d %d\n", f, g, h, i);

  printf("      *\n     *\n    *\n*   *\n * *\n  *\n");
  //  printf("%f %f %f %f %f", a, b, c, d, e);
  return 0;
}
